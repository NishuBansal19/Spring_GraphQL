type Query {
	helloWorld:String!
	greeting(firstName:String!, lastName:String): String!
	books: [Book]
	bookById(id:Int):Book
}

# book object type
type Book {
	id:Int,
	title:String!,
	totalPages:Int,
	rating:Float,
	isbn:String
}

